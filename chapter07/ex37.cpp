#include "ex37.h"

using std::cin;

Sales_data first_item(cin);		// Sales_data(std::istream&);

int main()
{
	Sales_data next;			// Sales_data(const std::string =  "");
	Sales_data last("9-999-99999-9");		// Sales_data(const std::string);

	return 0;
}
