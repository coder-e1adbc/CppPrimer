/*************************************/
									// int ix;
for (int ix = 0; ix != sz; ++ix) {}	// for (ix = 0; ix != sz; ++ix) {}	
if (ix != sz)

/*************************************/
int ix;
for (ix != sz; ++ix) {}				// for ( ; ix != sz; ++ix) {}

/*************************************/
for (int ix = 0; ix != sz; ++ix, ++sz) {}	// infinite loop ?
